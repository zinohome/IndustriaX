# 复制为 deploy/.env 并填入真实值。所有密钥必须设置，留空会导致服务启动失败（fail-closed，刻意为之）。
POSTGRES_PASSWORD=

# Dev: LLM/Embedding 走外部 Ollama 端点（本机无 GPU）
OLLAMA_BASE_URL=http://192.168.66.163:11434
# Dev: Reranker(TEI/bge-reranker-v2-m3) 建议部署在 192.168.66.163
RERANKER_BASE_URL=http://192.168.66.163:8080

# RAGFlow 自带栈密钥（必填，勿用默认值；若卷已存在需先轮换旧凭据）
MYSQL_PASSWORD=
MINIO_USER=
MINIO_PASSWORD=
VALKEY_PASSWORD=
